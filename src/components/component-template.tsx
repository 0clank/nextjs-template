import { FC as FunctionComponent } from 'react';

declare type ComponentTemplateProps = {}

export const ComponentTemplate: FunctionComponent<ComponentTemplateProps> = ({}) => {
    return (
        <>
        </>
    );
};